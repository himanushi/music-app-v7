import { IonContent, IonHeader, IonTitle, IonToolbar } from "@ionic/react";

export const Albums = () => {
  return (
    <IonContent>
      <IonHeader>
        <IonToolbar>
          <IonTitle>Albums</IonTitle>
        </IonToolbar>
      </IonHeader>
      albums
    </IonContent>
  );
};
